[tox]
envlist = py36
skipsdist = True

[testenv:reports]
passenv = TRAVIS TRAVIS_*
deps=
    pytest
    pytest-cov
    coveralls
    moto
commands=
    pip install -e .
    pip install -e ."[tests]"
    py.test --cov historical_reports
    coveralls
